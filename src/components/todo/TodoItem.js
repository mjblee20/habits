import React, { useState } from 'react';

// TODO: double click to change item to input for editing
function TodoItem() {
  const [editMode, toggleEditMode] = useState('false');
  return <div></div>;
}

export default TodoItem;
